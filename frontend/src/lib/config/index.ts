/**
 * Centralized configuration exports
 * Import this file to access all configuration objects
 */

export * from './categories'

// Re-export any existing config from this file that might already exist
// For example, if there's an existing searchClient config in lib/config.ts
// you would import and re-export it here 